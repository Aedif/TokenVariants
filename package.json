{
  "name": "token-variants",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "source:": "token-variants/token-variants.mjs",
  "main": "token-variants/bundle.js",
  "targets": {
    "main": {
      "optimize": true
    }
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  },
  "scripts": {
    "build": "npx parcel build token-variants/token-variants.mjs",
    "watch": "npx parcel watch token-variants/token-variants.mjs"
  }
}
